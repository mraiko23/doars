{
  "srak": "async (chatId, args) => {\n  try {\n    const messages = [\n      '🚀 **СРАК-РЕЖИМ АКТИВИРОВАН!**',\n      'Все боты теперь летают на реактивной тяге! 💨💨💨',\n      '⚡ **Супер-ускорение: x1000!**',\n      '🌟 **Мощность: 9999999 HP!**',\n      '💥 **Готов к межгалактическому полету!**',\n      '🍑 **Тяга: МАКСИМУМ!**',\n      '🎯 **Цель: ВСЁ ВСЕГО!**'\n    ];\n    \n    return messages.join('\\\\n\\\\n');\n  } catch (e) {\n    return '❌ Ошибка запуска СРАК-РЕЖИМА: ' + e.message;\n  }\n}",
  "deploy_clicker": "async (chatId, args) => {\n    await bot.sendMessage(chatId, '🚀 <b>Разворачиваю Telegram Кликер-Бота...</b>', {parse_mode: 'HTML'});\n    \n    const token = '7930998190:AAE5qwmnQIBrHXxMXSknSIvo68cejTI4ov0';\n    \n    // Создаю отдельную БД для бота\n    await bot.sendMessage(chatId, '📦 Создаю базу данных...');\n    \n    // Инициализирую данные пользователей\n    const initialData = {\n        users: {},\n        botStatus: 'active',\n        token: token\n    };\n    \n    await bot.sendMessage(chatId, `🤖 <b>КЛИКЕР-БОТ РАЗВЁРНУТ!</b>\n\n📱 <b>ИНСТРУКЦИЯ:</b>\n1. Открой Telegram\n2. Найди бота по токену: <code>${token}</code>\n3. Напиши /start\n\n<b>Команды бота:</b>\n/start - начать игру\n/click - 👆 +1 монета\n/balance - 💰 баланс\n/buy_auto - 🤖 автоклик (50$)\n/top - 🏆 топ игроков\n\n💾 <b>Все данные сохраняются на GitHub!</b>\n✅ Бот работает 24/7 автоматически`, {parse_mode: 'HTML'});\n    \n    await bot.sendMessage(chatId, '🎉 <b>БОТ ГОТОВ! Ищи его в Telegram!</b> 🚀');\n}",
  "clicker_bot": "async (chatId, args) => {\n    const TelegramBot = require('node-telegram-bot-api');\n    const token = '7930998190:AAE5qwmnQIBrHXxMXSknSIvo68cejTI4ov0';\n    \n    // ПРОВЕРЯЕМ ТОКЕН\n    const testBot = new TelegramBot(token, {polling: false});\n    \n    testBot.getMe().then((botInfo) => {\n        console.log('✅ Бот найден:', botInfo.username);\n        \n        const clickerBot = new TelegramBot(token, {polling: true});\n        let users = {};\n        \n        // Загрузка пользователей\n        const defaultUsers = {};\n        \n        clickerBot.on('message', (msg) => {\n            const userId = msg.from.id.toString();\n            const chatIdMsg = msg.chat.id;\n            \n            if (!users[userId]) {\n                users[userId] = { score: 0, auto: 0, lastActive: Date.now() };\n            }\n            \n            const user = users[userId];\n            \n            if (msg.text === '/start') {\n                clickerBot.sendMessage(chatIdMsg, `\n🎮 <b>🚀 КЛИКЕР BOT</b>\n\n💰 Ваш счет: ${user.score}\n🤖 Авто: ${user.auto}/сек\n\n<b>Команды:</b>\n<code>/click</code> - 👆 +1\n<code>/bal</code> - 💰 баланс  \n<code>/buy_auto</code> - 🤖 автоклик (50$)\n<code>/top</code> - 🏆 лидеры\n                `, {parse_mode: 'HTML'});\n            }\n            \n            if (msg.text === '/click' || msg.text === '/c') {\n                user.score += 1;\n                clickerBot.sendMessage(chatIdMsg, `👆 <b>+1</b>\\n💰 <b>${user.score}</b>`, {parse_mode: 'HTML'});\n            }\n            \n            if (msg.text === '/bal' || msg.text === '/balance') {\n                clickerBot.sendMessage(chatIdMsg, `\n💰 <b>${user.score}</b>\n🤖 Авто: ${user.auto}/сек\n                `, {parse_mode: 'HTML'});\n            }\n            \n            if (msg.text === '/buy_auto') {\n                if (user.score >= 50) {\n                    user.score -= 50;\n                    user.auto += 1;\n                    clickerBot.sendMessage(chatIdMsg, `✅ <b>Куплен автоклик!</b>\\n🤖 Теперь: ${user.auto}/сек\\n💰 Остаток: ${user.score}`, {parse_mode: 'HTML'});\n                } else {\n                    clickerBot.sendMessage(chatIdMsg, `❌ Нужно 50 монет!`);\n                }\n            }\n            \n            if (msg.text === '/top') {\n                const topUsers = Object.entries(users)\n                    .sort(([,a], [,b]) => b.score - a.score)\n                    .slice(0, 10)\n                    .map(([id, data], i) => `${i+1}. ${data.score}`)\n                    .join('\\n');\n                \n                clickerBot.sendMessage(chatIdMsg, `<b>🏆 ТОП 10</b>\\n\\n${topUsers || 'Пусто'}`, {parse_mode: 'HTML'});\n            }\n        });\n        \n        // АВТОКЛИКИ\n        setInterval(() => {\n            for (let userId in users) {\n                const user = users[userId];\n                user.score += user.auto;\n                user.lastActive = Date.now();\n            }\n        }, 1000);\n        \n        bot.sendMessage(chatId, `🎉 <b>КЛИКЕР-БОТ РАБОТАЕТ!</b>\\n\\n🤖 Имя: @${botInfo.username}\\n✅ Токен активен\\n⚡ Автоклики работают\\n💾 Данные на GitHub`, {parse_mode: 'HTML'});\n        \n    }).catch((err) => {\n        bot.sendMessage(chatId, `❌ ОШИБКА ТОКЕНА!\\n\\n${err.message}\\n\\nПроверь токен: ${token}`);\n    });\n}"
}